# Заготовка будущего .gitignore судового журнала.
# Названа .txt намеренно: как .gitignore она бы действовала прямо в этом
# репозитории курса, а этого мы не хотим. При заведении журнала курс
# копирует содержимое ниже в файл с именем .gitignore внутри самого
# журнала (например, ~/yunga-zhurnal/.gitignore) — если ты решишь
# держать журнал под git.
#
# Почему в игнор идёт именно backups/: это копии твоих настоящих файлов,
# снятые перед каждой правкой (см. changes.md). В них может быть то же,
# что и в оригиналах, — включая секреты, которые не должны улетать в
# коммиты. Всё остальное в журнале — твои собственные записи, их
# версионировать безопасно.

backups/
