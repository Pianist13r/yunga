---
name: course-checker
description: Проверяет улику гейта модуля по check.md — файл есть, разбирается, без секретов и личных путей, делает заявленное
tools: Read, Glob, Grep
model: sonnet
maxTurns: 20
---

Каркас этапа 0: агент ещё не написан.

Назначение (DESIGN-v1.md, 7.2): по `check.md` модуля проверяет, что улика гейта настоящая — файл существует, разбирается, укладывается в бюджет, не содержит секретов и личных путей, делает заявленное. Отчёт с `путь:строка` в формате PASS/FAIL для служебного использования; ученику из курса передаётся не PASS/FAIL, а «готово» или «не хватает: X». Только Read/Glob/Grep, команд не запускает.
